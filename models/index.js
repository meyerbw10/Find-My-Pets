const User = require('./User');


// Gallery.hasMany(Painting, {
//   foreignKey: 'animal_id',
// });

// Painting.belongsTo(Gallery, {
//   foreignKey: 'animal_id',
// });

module.exports = { User };
